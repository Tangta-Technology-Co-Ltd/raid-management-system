# RAID Management System

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Version](https://img.shields.io/badge/Version-1.0-blue.svg)]()
[![Platform](https://img.shields.io/badge/Platform-Linux-brightgreen.svg)]()

## 概述

RAID 管理系统是一个专为 Linux 操作系统设计的综合性软件 RAID 阵列管理套件。它提供先进的监控、管理和自动化功能，特别针对外部 USB RAID 设备进行优化，确保系统在设备未连接时仍能正常启动。

## 主要特性

### 🚀 核心功能
- **多阵列管理** - 支持 md0, md1, md2 等多个 RAID 阵列
- **智能启动/停止** - 自动处理阵列组装和拆卸
- **实时监控** - 可视化重新同步进度和性能指标
- **健康诊断** - 全面的状态报告和故障检测

### 💾 自动化文件系统管理
- **智能检测** - 自动识别缺失的文件系统
- **交互式创建** - 支持 ext4、xfs、btrfs 等多种文件系统
- **自动挂载** - 智能挂载点管理和磁盘使用报告
- **零配置使用** - 从阵列启动到挂载完成的完整自动化流程

### 🛡️ 安全可靠性
- **安全启动** - 外部设备缺失时防止启动阻塞
- **优雅降级** - 缺失阵列组件的智能处理
- **数据完整性** - 多重验证层和保护机制
- **错误恢复** - 自动化故障恢复程序

### 🎯 用户体验
- **彩色终端输出** - 易于识别的状态指示
- **Shell 别名集成** - 快速命令访问
- **详细帮助系统** - 全面的使用文档和示例
- **交互式提示** - 用户友好的操作引导

## 快速开始

### 系统要求
- **操作系统**: Linux 内核 4.4+
- **Shell 环境**: Bash 4.3+
- **依赖软件**: mdadm
- **内存**: 最小 512MB RAM

### 安装步骤

#### 方法一：快速安装
```bash
# 下载最新版本
wget https://github.com/Tangta-Technology-Co-Ltd/raid-management-system/releases/download/v1.0/raid-management-1.0.tar.gz

# 解压并安装
tar -xzf raid-management-1.0.tar.gz
cd raid-management-1.0
sudo ./install.sh

# 配置用户环境
raid-setup-aliases
source ~/.bashrc
```

#### 方法二：手动安装
```bash
# 复制脚本文件
sudo cp scripts/* /usr/local/bin/
sudo chmod +x /usr/local/bin/raid-* /usr/local/bin/usb-raid-*

# 安装文档
sudo mkdir -p /usr/local/share/raid-management
sudo cp LICENSE README.md COPYRIGHT /usr/local/share/raid-management/
```

### 基本使用

```bash
# 检查系统状态
raid-status

# 启动阵列（自动检测和创建文件系统）
raid-start-md0

# 监控健康状态
raid-health

# 停止阵列
raid-stop-md0
```

## 命令参考

### 核心命令
| 命令 | 描述 | 示例 |
|------|------|------|
| `raid-start` | 启动所有阵列 | `raid-start` |
| `raid-stop` | 停止所有阵列 | `raid-stop` |
| `raid-status` | 状态概览 | `raid-status` |
| `raid-health` | 健康检查 | `raid-health` |
| `raid-progress` | 进度监控 | `raid-progress` |
| `raid-list` | 列出所有阵列 | `raid-list` |

### 阵列特定命令
| 命令 | 描述 | 示例 |
|------|------|------|
| `raid-start-md0` | 启动 md0 阵列 | `raid-start-md0` |
| `raid-stop-md0` | 停止 md0 阵列 | `raid-stop-md0` |
| `raid-mount0` | 挂载 md0 阵列 | `raid-mount0` |
| `raid-umount0` | 卸载 md0 阵列 | `raid-umount0` |

### 挂载管理
| 命令 | 描述 | 示例 |
|------|------|------|
| `usb-raid-mount` | 挂载指定阵列 | `usb-raid-mount 0` |
| `usb-raid-umount` | 卸载指定阵列 | `usb-raid-umount 0` |
| `raid-mounts` | 显示挂载状态 | `raid-mounts` |

## 高级功能

### 多阵列管理
```bash
# 同时管理多个阵列
raid-start-md0
raid-start-md1
raid-status

# 分别挂载到不同目录
raid-mount0  # → /mnt/usb-raid0
raid-mount1  # → /mnt/usb-raid1
```

### 外部 USB RAID 支持
```bash
# 安全处理外部设备
# - 设备断开时不会阻塞系统启动
# - 重新连接后可以手动启动阵列
raid-start-md0
```

### 实时监控
```bash
# 可视化重新同步进度
raid-progress

# 输出示例：
# [████████████████░░░░░░░░] 65.2%
# 剩余时间: 45.3分钟
# 速度: 205.7MB/秒
```

## 配置说明

### 持久化配置
```bash
# 保存 RAID 配置
sudo mdadm --detail --scan | sudo tee -a /etc/mdadm/mdadm.conf

# 防止外部设备启动阻塞
echo "AUTO -all" | sudo tee -a /etc/mdadm/mdadm.conf
sudo update-initramfs -u
```

### 自动挂载配置
```bash
# 获取阵列 UUID
sudo blkid /dev/md0

# 添加到 fstab (替换实际 UUID)
echo "UUID=your-uuid-here /mnt/usb-raid0 ext4 defaults 0 2" | sudo tee -a /etc/fstab
```

## 故障排除

### 常见问题

**阵列无法启动**
```bash
# 检查组件状态
sudo mdadm --examine /dev/nvme0n1p2

# 手动组装
sudo mdadm --assemble /dev/md0 /dev/nvme0n1p2 /dev/nvme1n1p2 --run
```

**权限问题**
```bash
# 确保脚本可执行
sudo chmod +x /usr/local/bin/raid-*
```

**挂载失败**
```bash
# 检查文件系统
sudo blkid /dev/md0

# 创建文件系统
sudo mkfs.ext4 /dev/md0
```

### 日志检查
```bash
# 查看系统日志
sudo dmesg | grep md
sudo journalctl -u mdadm

# 检查内核消息
dmesg | grep -i raid
```

## 开发信息

### 项目结构
```
raid-management-system/
├── install.sh              # 安装脚本
├── uninstall.sh           # 卸载脚本
├── LICENSE               # MIT 许可证
├── README.md             # 项目文档
├── COPYRIGHT             # 版权信息
├── scripts/              # 主程序脚本
│   ├── raid-health       # 健康检查
│   ├── raid-status       # 状态监控
│   ├── raid-start        # 启动管理
│   ├── raid-stop         # 停止管理
│   ├── raid-progress     # 进度监控
│   ├── raid-list         # 阵列列表
│   ├── raid-mounts       # 挂载状态
│   ├── usb-raid-mount    # 挂载管理
│   └── usb-raid-umount   # 卸载管理
├── share/                # 文档资源
│   └── raid-management/
│       ├── examples/     # 使用示例
│       └── config-examples/ # 配置示例
└── bin/                  # 用户工具
    └── setup-aliases.sh  # 别名设置
```

### 技术特性
- **开发语言**: Bash Shell 脚本
- **兼容系统**: Ubuntu, Debian, CentOS, RHEL, Fedora, Arch Linux
- **RAID 支持**: 软件 RAID (mdadm) 所有级别
- **文件系统**: ext4, xfs, btrfs 等
- **硬件架构**: x86-64, ARM

## 支持与贡献

### 问题报告
如果您遇到任何问题，请通过以下方式报告：
1. 在 GitHub 仓库创建 Issue
2. 提供详细的错误信息和系统环境
3. 包含相关的日志输出

### 功能建议
我们欢迎功能建议和改进意见！请通过 GitHub Issues 提交您的想法。

### 开发贡献
如果您想为项目贡献代码：
1. Fork 本仓库
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 版权信息

Copyright (c) 2025 唐塔科技有限公司 (Tangta Technology Co., Ltd.)
版权所有，保留所有权利。

## 更新日志

### v1.0 (2025-01-01)
- ✨ 初始版本发布
- 🎯 多阵列 RAID 管理支持
- 🔄 自动化文件系统创建和挂载
- 📊 实时进度监控和健康诊断
- 🛡️ 安全启动保护和错误恢复

---

**唐塔科技有限公司** - 专注于企业级存储解决方案和技术创新。

*让存储管理更简单，更可靠。*
